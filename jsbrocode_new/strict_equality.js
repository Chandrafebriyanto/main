// = digunakan untuk menyatakan variabel
// == membandingkan antar data apakah sama (tanpa tipe data)
// === membandingkan antar nilai dan tipe data apakah sama
// != tidak sama dengan
// !=== tidak sama dengan (nilai data dan tipe data)

const PI = 3.14;

if (PI !== 3.14){
    console.log("ini adalah nilai PI")
}
else{
    console.log("ini bukan nilai PI")
}